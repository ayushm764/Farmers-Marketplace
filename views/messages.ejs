<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Messages</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="container">
        <h1>Messages</h1>
        <% if (messages.length> 0) { %>
            <% messages.forEach(message=> { %>
                <div class="message-card">
                    <img src="<%= message.contractor.imageUrl %>" alt="Contractor Image" class="contractor-img">
                    <div class="message-info">
                        <h3>
                            <%= message.contractor.name %>
                        </h3>
                        <p><strong>Latest Message:</strong>
                            <%= message.latestMessage.content %>
                        </p>
                        <p class="timestamp">
                            <%= new Date(message.latestMessage.timestamp).toLocaleString() %>
                        </p>
                        <a href="/contractor/chat/<%= message.contractor._id %>" class="view-chat">View Chat</a>
                    </div>
                </div>
                <% }); %>
                    <% } else { %>
                        <p>No messages found.</p>
                        <% } %>
    </div>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            text-align: center;
        }

        .container {
            width: 80%;
            margin: 20px auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .message-card {
            display: flex;
            align-items: center;
            border: 1px solid #ddd;
            padding: 15px;
            margin: 10px 0;
            background: #fff;
            border-radius: 8px;
        }

        .contractor-img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            margin-right: 15px;
        }

        .message-info {
            flex-grow: 1;
            text-align: left;
        }

        .timestamp {
            font-size: 12px;
            color: gray;
        }

        .view-chat {
            text-decoration: none;
            color: white;
            background: #007bff;
            padding: 5px 10px;
            border-radius: 5px;
            display: inline-block;
            margin-top: 5px;
        }

        .view-chat:hover {
            background: #0056b3;
        }
    </style>
</body>

</html>